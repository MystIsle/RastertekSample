# Shaders

HLSL 셰이더를 감싸는 C++ 래퍼 클래스. HLSL 원본은 `assets/shaders/`에 있다.

튜토리얼을 완주하면 20개 이상 생기므로 `Graphics/`와 분리해 둔다.

예정: `ColorShaderClass`(튜토리얼 4), `TextureShaderClass`(5), `LightShaderClass`(6~7), `BumpMapShaderClass`, `SpecMapShaderClass` ...
